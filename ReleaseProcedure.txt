*. Confirm release number correct in Version Number.txt

*. Clear out existing installation files at
	/usr/local/ssl/Release-iphoneos

*. Perform clean build of project
	(do sudo ./cleanAll/sh if necessary first)

*. Check operation - see ReleaseTesting.xlsx

*. Perform release:
	./doRelease Vx.y.z_mm-dd-yy

*. Install release files:
	sudo ./installFipsRelease.sh

*. Check operation - see ReleaseTesting.xlsx

*. Copy release files to shared drive:
	/Volumes/shares/Groups/Tech Team/Current Projects/FIPS/release/FCIDS
	Update /Users/scoleman/dev/IOSFipsBuilds/test1020/fcids/ReleaseProcedure.txt

*. Create a git branch corresponding to release (Branch named Vx.y.z_mm-dd-yy)